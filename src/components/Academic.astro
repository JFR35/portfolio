<section id="formacion" class="section bg-academia py-5 text-white">
    <div class="container">
      <h2 class="text-center mb-5 retro-text">Formación Académica</h2>
      <div class="row justify-content-center align-items-center">
        <!-- Bloque con la información de formación académica -->
        <div class="col-md-10">
          <div class="d-flex flex-column flex-md-row align-items-md-center academy-layout">
            <!-- Icono o elemento decorativo a la izquierda -->
            <div class="icon-container text-center mb-4 mb-md-0 col-md-2">
              <i class="fas fa-graduation-cap fa-4x text-light"></i>
            </div>
            <!-- Texto de la formación académica -->
            <div class="col-md-10">
              <ul class="list-unstyled text-left academic-details">
                <li class="mb-4">
                  <h4 class="retro-text">Grado Superior en Desarrollo de Aplicaciones Web (DAW)</h4>
                  <p class="mb-0 fs-5">Universidad Alfonso X el Sabio (UAX).</p>
                </li>
                <li class="mb-4">
                    <h4 class="retro-text">Especialista en Entornos Sanitarios y Certificado en OpenEHR</h4>
                    <p class="mb-0 fs-5">Universidad Alfonso X el Sabio (UAX).</p>
                </li>
                <li class="mb-4">
                  <h4 class="retro-text">Máster en Formación Pedagógica y Didáctica</h4>
                  <p class="mb-0 fs-5">Universidad Alfonso X el Sabio (UAX).</p>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

<style>
    /* Fondo único para la sección de formación */
.bg-academia {
  background-color: #1d3259; /* Azul más vibrante que el fondo estándar */
  background-image: 
    radial-gradient(circle at 20% 50%, rgba(80, 120, 180, 0.8), transparent 60%), 
    radial-gradient(circle at 80% 70%, rgba(100, 150, 200, 0.6), transparent 70%);
  background-size: cover;
  background-position: center;
  background-attachment: fixed;
}

/* Estilo para el layout de la formación */
.academy-layout {
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 10px;
  padding: 20px;
  box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
  background-color: rgba(0, 0, 0, 0.3);
  transition: transform 0.3s, box-shadow 0.3s ease-in-out;
}

.academy-layout:hover {
  transform: scale(1.03);
  box-shadow: 0 0 30px rgba(255, 255, 255, 0.4);
}

/* Detalles académicos */
.academic-details h4 {
  font-family: var(--font-retro);
  color: var(--text-light);
  margin-bottom: 5px;
}

.academic-details p {
  color: #d3d9e8;
  font-size: 1rem;
}

/* Icono decorativo */
.icon-container i {
  color: rgba(255, 255, 255, 0.8);
  transition: color 0.3s ease-in-out;
}

.icon-container i:hover {
  color: #3498db; /* Cambia a un azul brillante */
}

</style>